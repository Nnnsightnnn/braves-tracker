import React from 'react'
import ReactDOM from 'react-dom/client'
import BravesTracker from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BravesTracker />
  </React.StrictMode>,
)
